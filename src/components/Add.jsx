import React from "react";
import "./add.css";
const Add = ({ inputList }) => {
  return (
    <div className="add">
      <ol className="listcol">{console.log(inputList)}</ol>
    </div>
  );
};

export default Add;
